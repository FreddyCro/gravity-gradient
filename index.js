const config = {
  positiveColor: ['', ''],
  negativeColor: '',
  breakpoint: 1024,
};

const commonStyle = {
  'will-chage': 'transform',
};

// requestAnimationFrame

const gg = document.querySelector('#gg-halo');
